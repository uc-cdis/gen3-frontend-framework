// Type stub for the vendored @tailwindcss/typography plugin (see ./index.js).
// The plugin is consumed only inside a Tailwind `plugins` array, so `any` is sufficient.
declare const typography: any;
export default typography;
