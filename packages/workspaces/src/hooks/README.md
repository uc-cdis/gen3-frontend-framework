# Original hooks from the vectis monorepo

The refactoring of the hooks is still in progress. Trying to migrate to using RTK Query instead of direct fetch calls.

Also refactoring these hooks to ContextProviders in the `../providers` directory.
