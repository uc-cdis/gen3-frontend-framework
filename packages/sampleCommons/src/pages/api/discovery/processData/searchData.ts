// Search Results based on a query
const searchData = (data, query) => {
  // return data.filter((item) => item.title.includes(query)); // Example search
  return data;
};

export default searchData;
